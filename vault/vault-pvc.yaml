apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: vault-pvc
  namespace: security-lab

spec:

  storageClassName: standard

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi